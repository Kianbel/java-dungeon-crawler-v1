package gui;

// Add this at the bottom of GameController.java or as a package-private class
class RenderOffset {
    double x = 0.0;
    double y = 0.0;

    public RenderOffset(double x, double y) {
        this.x = x;
        this.y = y;
    }
}